import '../constants/api_constants.dart';

class AppConfig {
  static const appName = 'Transport Driver';

  static final baseUrl = ApiConstants.baseUrl;

  static const timeout = Duration(seconds: 30);
}